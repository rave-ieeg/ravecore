% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validator-condition_groups.R
\name{validate_condition_groupings}
\alias{validate_condition_groupings}
\title{Validate and normalize a list of user-supplied condition groupings.}
\usage{
validate_condition_groupings(condition_groupings, epoch = NULL)
}
\arguments{
\item{condition_groupings}{A list of condition group items. Each item is
typically a list with elements \code{label} and \code{conditions}.}

\item{epoch}{Optional. A \code{RAVEEpoch} object (with \verb{$table}) or a
\code{data.frame} containing a \code{Condition} column. When supplied, conditions
that do not appear in the epoch's \code{Condition} column are dropped.}
}
\value{
A list with the following elements:
\itemize{
\item \code{n} - the number of valid condition groups remaining after cleaning;
\item \code{groups} - an unnamed list of cleaned group items (see below);
\item \code{group_indexes} - integer vector of the original 1-based positions
of the kept groups (used to derive a stable color);
\item \code{group_labels} - character vector of the cleaned group labels;
\item \code{unique_conditions} - sorted unique condition names across all groups;
\item \code{use_epoch} - logical, \code{TRUE} when \code{epoch} was supplied;
\item \code{n_trials} - (only when \code{epoch} is supplied) integer vector of the
number of trials in each group.
}

Each cleaned group item contains:
\itemize{
\item \code{index} - original 1-based position of the group in the input;
\item \code{label} - cleaned label (auto-filled if missing/empty);
\item \code{conditions} - unique character vector, in input order (not sorted);
\item (when \code{epoch} is supplied)
\itemize{
\item \code{trials_by_condition} - named list of integer trial numbers from
the epoch \code{Trial} column, one element per condition;
\item \code{trial_count} - integer vector of trial counts per condition;
\item \code{trials_included} - integer vector of trial numbers ordered by
condition (concatenated in the condition order of \code{conditions});
\item \code{n_trials} - total number of trials in the group.
}
}
}
\description{
Groups with empty or no conditions (after filtering against \code{epoch}, when
provided) are removed from the result.
}
\examples{


condition_groupings <- list(

  # Normal case
  list(
    label = "AOnly",
    conditions = c("drive_a", "known_a", "last_a", "meant_a")
  ),

  # Invalid/empty label
  list(
    label = NA,
    conditions = c("last_av", "drive_av", "known_av", "meant_av")
  ),

  # Empty condition
  list(
    label = "G3"
  ),

  # Missing label
  list(
    conditions = c("known_v", NA)
  ),

  # Invalid condition if epoch is provided
  list(
    conditions = "adafawefwf"
  )
)

# Without epoch
validate_condition_groupings(condition_groupings)

\dontrun{

# With epoch table: run `ravecore::install_subject("DemoSubject")`
# to install the demo-subject
subject <- ravecore::new_rave_subject("demo", "DemoSubject")
epoch <- subject$get_epoch("auditory_onset")

validate_condition_groupings(condition_groupings, epoch)

}


}
